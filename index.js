let myLibrary = [];

function Book(title, author) {
    this.title = title;
    this.author = author;
}

function addBookToLibrary() {
    const userBook = prompt("Add a book to the library", "War and Peace");
}

//addBookToLibrary();